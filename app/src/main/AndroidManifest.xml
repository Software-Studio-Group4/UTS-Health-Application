<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uts.group4.UTShealth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Dashboard"></activity>
        <activity android:name=".DatabaseTester">
            <intent-filter>
                <action android:name="com.company.package.FOO"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homepage"></activity>
        <activity android:name=".Login"></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".RegisterEmailPge" />
        <activity android:name=".RegisterPassPge" />
        <activity android:name=".RegisterDetailsPge" />
        <activity android:name=".RegisterFinishPge" />
        <activity android:name=".Chat" />
    </application>

</manifest>